using UnityEngine;

namespace __Scripts.Provider
{
    public class ConfigProvider : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
